// @author: danielmoffat
//
// Some extremely useful utlities.

'use strict';

var utils = {
	add: add,
  subtract: subtract
};

module.exports = utils;

function add(a, b) {
	return a + b;
}

function subtract(a, b) {
  return a - b;
}
